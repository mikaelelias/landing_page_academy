function agenda() {
    var htmlElement = document.querySelector('.formulario_aula');
    htmlElement.scrollIntoView({ behavior: "smooth", block: "end", inline: "nearest"})
}

function pacotes() {
    window.scrollBy(0, 600);
    scrollButton.removeEventListener('click', scrollTo100px);
}
